
# Cleaned requirements for Streamlit deployment (UI & core features only)
streamlit==1.28.2
plotly==5.17.0
pandas==2.1.4
numpy==1.25.2

# Optional: If you use these features in your app, uncomment as needed
 streamlit-option-menu==0.3.6
 streamlit-plotly-events==0.0.6
streamlit-aggrid==0.3.4
 altair==5.2.0
folium==0.15.1
streamlit-folium==0.15.0
requests==2.31.0
pillow==10.1.0

# If you need database or AI/ML, add only what is required for your app UI
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
openai==1.3.8
scikit-learn==1.3.2

# Remove heavy/complex packages for initial deployment (add back if needed and test locally first)
prophet
torch
tensorflow-lite
onnxruntime
roberta
openai-offline